name: Test Pipeline

on:
  push:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Say hello
        run: echo "GitHub Actions is working!"

      - name: Show files
        run: ls -la